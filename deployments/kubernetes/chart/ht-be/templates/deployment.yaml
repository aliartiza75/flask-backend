apiVersion: apps/v1
kind: Deployment
metadata:
  name: {{ include "ht-be.fullname" . }}
  labels:
    app.kubernetes.io/name: {{ include "ht-be.name" . }}
    helm.sh/chart: {{ include "ht-be.chart" . }}
    app.kubernetes.io/instance: {{ .Release.Name }}
    app.kubernetes.io/managed-by: {{ .Release.Service }}
spec:
  replicas: {{ .Values.replicaCount }}
  selector:
    matchLabels:
      app.kubernetes.io/name: {{ include "ht-be.name" . }}
      app.kubernetes.io/instance: {{ .Release.Name }}
  template:
    metadata:
      labels:
        app.kubernetes.io/name: {{ include "ht-be.name" . }}
        app.kubernetes.io/instance: {{ .Release.Name }}
    spec:
      containers:
        - name: {{ .Chart.Name }}
          image: "{{ .Values.image.repository }}:{{ .Values.image.tag }}"
          imagePullPolicy: {{ .Values.image.pullPolicy }}
          env: 
            - name: "FLASK_ENV"
              value: {{ .Values.deploymentInfo.envName }}
            - name: "FLASK_HOST_IP"
              value: {{ .Values.deploymentInfo.hostIP }}
            - name: "FLASK_HOST_PORT"
              value: {{ .Values.deploymentInfo.hostPort }}
            - name: "Name"
              value: {{ .Values.deploymentInfo.userName }}
          ports:
            - name: http
              containerPort: {{ .Values.deploymentInfo.hostPort }}
              protocol: TCP
          livenessProbe:
            httpGet:
              path: /
              port: "9000"
          readinessProbe:
            httpGet:
              path: /
              port: "9001"
          resources:
            {{- toYaml .Values.resources | nindent 12 }}
      {{- with .Values.nodeSelector }}
      nodeSelector:
        {{- toYaml . | nindent 8 }}
      {{- end }}
    {{- with .Values.affinity }}
      affinity:
        {{- toYaml . | nindent 8 }}
    {{- end }}
    {{- with .Values.tolerations }}
      tolerations:
        {{- toYaml . | nindent 8 }}
    {{- end }}
